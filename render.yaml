services:
  - type: web
    name: accessedu-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python AccessEdu_Project.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 8000
